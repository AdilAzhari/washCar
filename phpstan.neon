includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app
        - config
        - database
        - routes

    level: 3

    ignoreErrors:
        - '#PHPDoc tag @var#'

    excludePaths:
        - bootstrap
        - storage
        - vendor
        - node_modules (?)
